# QuadrayGrid4 Saves

Save files from QuadrayGrid4 version of the browser application.

## Files

JSON save files with naming pattern:

```
quadcraft_quadgrid4_{timestamp}.json
```

See parent directory for save format documentation.
