# quadraygrid5/ — Agent Notes

JSON save files from the QuadrayGrid5 version of the browser app. Same naming
pattern and handling rules as `../quadraygrid4/` (see its AGENTS.md). User data —
do not edit or rename.
