# saves/ — Agent Notes

Two JSON save files from the experiments page plus its README (contract already
documented). User data — never edit; treat as reproducibility evidence for the
experiments build.
