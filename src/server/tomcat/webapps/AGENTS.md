# webapps/ — Agent Notes

Standard Tomcat webapps directory; single webapp `QuadCraft/` (README already
documents the contract). Deploy target for the Java-backed serving path — the
main browser game in `src/browser/` does not need Tomcat.
