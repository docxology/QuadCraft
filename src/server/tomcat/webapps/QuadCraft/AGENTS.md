# QuadCraft/ — Agent Notes

Tomcat webapp for QuadCraft. `WEB-INF/classes/` holds compiled Java + source
(`immutable/occamsjsonds/` — the OccamsJSONDS JSON datastore, `.java` + `.class`).
Static serving layout; classes here mirror the Java source, keep them in sync.
