# WEB-INF/ — Agent Notes

Standard servlet-private directory: `classes/` contains the Java classes and
sources used by the QuadCraft webapp. Content under WEB-INF is not directly
served — only through the application.
