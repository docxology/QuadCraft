# immutable/ — Agent Notes

Package root for the immutable datastore classes: only `occamsjsonds/`
(OccamsJSONDS, source + compiled). Name is the Java package path — moving these
files breaks the package declaration.
