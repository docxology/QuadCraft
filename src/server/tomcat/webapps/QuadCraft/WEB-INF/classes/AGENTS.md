# classes/ — Agent Notes

Compiled-classes root for the QuadCraft webapp. Single child `immutable/`
(OccamsJSONDS JSON datastore: .java source + .class artifacts side by side).
After any `.java` edit, recompile or the `.class` goes stale.
