# core/ — Agent Notes

Python core for the games infrastructure: `config.py` (configuration),
`registry.py` (game registry). Consumed by the launcher and tests under
`../../tests/`. `__pycache__/` present — ignore.
