# tests/ — Agent Notes

Node-run unit tests for the 4d_catan game modules (`../js/`). Run from the repo
root via the game harness: `python3 games/run_games.py --test --game catan`
(harness and registry game id verified in `games/run_games.py` /
`games/src/core/registry.py`, 2026-08-29).
