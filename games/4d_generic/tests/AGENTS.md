# tests/ — Agent Notes

Node-run tests for the shared 4d_generic library: `test_quadray.js`,
`test_synergetics.js` (verified 2026-08-29). Run directly:
`node games/4d_generic/tests/test_quadray.js` from the repo root. These test the
canonical quadray/synergetics math that every game depends on — changes to
`../` shared modules should keep these green first.
