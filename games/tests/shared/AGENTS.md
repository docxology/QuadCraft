# shared/ — Agent Notes

Node tests for the shared 4d_generic modules and HUD (`test_base_board.js`,
`test_base_game.js`, `test_base_renderer.js`, `test_camera.js`,
`test_entity_system.js`, `test_grid_utils.js`, `test_hud.js`,
`test_all_shared.js`). Mirror coverage of the library also lives in
`../../4d_generic/tests/`; keep both green when touching shared code.
